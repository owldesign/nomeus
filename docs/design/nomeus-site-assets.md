# nomeus.dev — asset list

Captures from the (redesigned) dashboard, dark theme, 2× retina PNG. Keep chrome out — the page adds its own browser frame.

1. **hero-services** — Services page. 1600×1000 (shows at ~520×340). 11/11 running, one row expanded showing the .env block + log tail. This is the money shot: the flock of star LEDs must be visible.
2. **fact-debug** — Debug page. 1200×660 (shows at ~460×220). One dump row open with the lantern arrive-wash if you can catch it; Queries tab showing a non-zero count.

Optional (if the hero becomes a loop): 6–8s capture of a `services:clone` task streaming, ≤2 MB, muted, `prefers-reduced-motion` fallback = the still above.

Fonts to bundle in `site/fonts/` (both OFL): GeistMono[wght].woff2, PublicSans[wght].woff2.
