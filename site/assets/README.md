# Screenshots for nomeus.dev

Two PNGs, dark theme, 2× retina, no browser chrome (the page draws its own frame). Until they exist the page hides the images.

- `hero-services.png` — 1600×1000: the Services page, all instances running, one row expanded (the .env block and log tail visible).
- `fact-debug.png` — 1200×660: the Debug page with a dump row open and the Queries tab showing a count.

Keep both under ~180 KB combined (`pngquant` or `oxipng` help). See `docs/design/nomeus-site-assets.md`.
