<?php

namespace App\Services\Services;

/**
 * A driver that runs one of nomeus's own artisan commands as a service (the dump server).
 * Like SiteBound, but the "site" is nomeus itself: its php, its directory, no --site needed.
 */
interface NomeusBound {}
