## What

<!-- one paragraph: the behaviour before, the behaviour after -->

## Checklist

- [ ] `vendor/bin/pest` passes; new behaviour has tests, and every process / HTTP call in them is faked
- [ ] `php artisan docs:commands` re-run if a command or option changed
- [ ] a line for `CHANGELOG.md` is not needed — the commit subject is the changelog entry (`area: what changed`)
- [ ] ran it on a real Mac (say which macOS / PHP) — or say that you couldn't
